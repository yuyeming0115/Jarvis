# V2.1 晨间测试清单

目标：2026-07-04 10:00 前确认 Jarvis 可以先作为个人任务入口使用。

## 1. 打开工作台

```bash
open http://127.0.0.1:8080/
```

确认页面能打开，并能看到任务列表。

## 2. 检查本地服务

```bash
bash "$HOME/Jarvis/services/status-launchd.sh"
bash "$HOME/Jarvis/services/api-smoke-test.sh"
```

重点确认：

- `com.local.jarvis.workbench` 是 `running`
- `com.local.jarvis.reminders` 是 `running`
- API 检查通过

## 3. 测试飞书入口

在飞书里给 `Jarvis 助手` 发：

```text
明天 10 点提醒我复盘 Jarvis
```

然后打开工作台，确认新任务出现，且到期时间为明天 10:00。

## 4. 快速回归脚本

```bash
bash "$HOME/Jarvis/services/verify-feishu-flow.sh"
```

看到 `Feishu flow verified` 即通过。

## 5. 提醒服务干跑

```bash
cd "$HOME/Jarvis"
python3 -m backend.core.reminders --once --dry-run
```

没有到期任务时应显示：

```text
due_tasks=0
```

## 当前边界

- 飞书使用临时 `loca.lt` 公网隧道，适合测试，不是长期生产方案。
- macOS 通知到期后只提醒一次。
- 暂不处理飞书 Encrypt Key 加密事件。
- OpenClaw 和 Hermes 暂未安装，等提醒闭环稳定后再接。
